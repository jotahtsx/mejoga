import styled from 'styled-components'

export const Wrapper = styled.header`
  width: 100%;
  height: 3.125rem;
  background-color: var(--primary);
  .container {
    max-width: 74rem;
    margin: 0 auto;
  }
`
